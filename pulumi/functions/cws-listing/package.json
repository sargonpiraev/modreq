{
  "name": "cws-listing-etl",
  "version": "0.0.0",
  "private": true,
  "description": "Daily Chrome Web Store public listing snapshot → BigQuery product_cws",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "pack:deploy": "npm run build && rm -rf deploy && mkdir -p deploy/lib && node ./scripts/write-deploy-package.mjs && cp -R lib/. deploy/lib/",
    "start": "node lib/cli.js",
    "smoke": "npm run build && node lib/cli.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@google-cloud/bigquery": "^7.9.4"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0"
  }
}

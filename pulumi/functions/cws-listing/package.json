{
  "name": "cws-listing-etl",
  "version": "0.0.0",
  "private": true,
  "description": "Daily Chrome Web Store public listing snapshot → BigQuery cws",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "pack:deploy": "npm run build && rm -rf deploy && mkdir -p deploy/lib && node ./scripts/write-deploy-package.mjs && cp -R lib/. deploy/lib/",
    "smoke": "npm run build && node lib/cli.js",
    "start": "node lib/cli.js"
  },
  "dependencies": {
    "@google-cloud/bigquery": "^7.9.4"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}

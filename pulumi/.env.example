# Copy to .env (gitignored). Tokens only — never commit.
# GCP BQ + Cloud Functions deployer (goproj@… — same as meta pulumi/.env GCP_SERVICE_ACCOUNT_KEY)
GCP_SERVICE_ACCOUNT_KEY=

# Local file backend — stack state under pulumi/.pulumi
# From this directory: file://$PWD
PULUMI_BACKEND_URL=file://$PWD
PULUMI_CONFIG_PASSPHRASE=

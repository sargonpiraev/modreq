# modreq

Chrome extension to modify request headers and cookies (WXT / MV3). Free ModHeader alternative.

Sibling project under the private meta workspace (`projects/`). Portfolio governance (lefthook, semantic-release, test targets, `extapp` conventions) lives in meta `AGENTS.md` and `.cursor/rules/`.
